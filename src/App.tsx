import React, { FC } from "react";
import RenderLevel from "./components/RenderLever";

const App: FC = (): JSX.Element => {
  return (
    <div className="App">
      <RenderLevel />
    </div>
  );
};

export default App;
